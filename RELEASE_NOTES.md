# Quota Bar 1.1.0

- 新增智能刷新：工作中每 30 秒、全部空闲时每 5 分钟；也可选择 30 秒、1 分钟、5 分钟或仅手动。
- 新增更细的本地工作状态：等你审批、当牛马中、思考中、摸鱼中、未运行、需处理。
- 新增中文 / English 即时切换。
- Claude Code 增加纯本地 command hooks，用于捕获审批、工作和空闲状态。
- 保持零模型调用：不发送测试提示词，不使用 prompt/agent hooks，不调用模型来判断状态。

安装方式：打开 DMG，将 `Quota Bar.app` 拖入 Applications。首次打开若被 macOS 拦截，请在“系统设置 → 隐私与安全性”中允许打开。
